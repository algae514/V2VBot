fastapi>=0.118.0
uvicorn[standard]>=0.37.0
aiortc>=1.13.0
pydantic>=2.11.0
aiofiles>=24.1.0
httpx[http2]>=0.27.0
numpy>=1.26.0
soundfile>=0.13.0
opuslib>=3.0.1
faster-whisper>=1.2.0
google-generativeai>=0.8.0
python-dotenv>=1.1.0

# Text-to-Speech (MeloTTS) - Local GPU-accelerated TTS
# MeloTTS is installed separately in setup scripts with --no-deps to avoid conflicts
# Core dependencies for MeloTTS:
transformers>=4.30.0
soxr  # SoX Resampler library (audio resampling)
hf_transfer  # High-speed transfer for HuggingFace models
nltk  # Natural Language Toolkit (for text processing)

# MeloTTS dependencies (installed separately in setup.sh to avoid conflicts)
# These are listed here for reference but installed via setup script:
# anyascii==0.3.2
# cached_path
# cn2an==0.5.22
# eng_to_ipa==0.0.2
# fugashi==1.3.0
# g2p_en==2.1.0
# g2pkk>=0.1.1
# gruut[de,es,fr]==2.2.3
# inflect==7.0.0
# jamo==0.4.1
# jieba==0.42.1
# langid==1.1.6
# librosa==0.9.1
# loguru==0.7.2
# mecab-python3==1.0.9
# num2words==0.5.12
# pydub==0.25.1
# pykakasi==2.2.1
# pypinyin==0.50.0
# tensorboard==2.16.2
# txtsplit
# unidecode==1.3.7
# unidic==1.1.0
# unidic_lite==1.0.8

# GPU-accelerated dependencies
# Install PyTorch with CUDA 12.1 support
--extra-index-url https://download.pytorch.org/whl/cu121
torch>=2.1.0
torchaudio>=2.1.0

# ONNX Runtime with CUDA support for Silero VAD
# Note: Do NOT install both onnxruntime and onnxruntime-gpu - they conflict
# Uninstall onnxruntime before installing onnxruntime-gpu
onnxruntime-gpu>=1.16.0

# CTranslate2 with CUDA support for Faster-Whisper
# Note: faster-whisper will automatically use GPU if available
