fastapi>=0.118.0
uvicorn[standard]>=0.37.0
aiortc>=1.13.0
pydantic>=2.11.0
aiofiles>=24.1.0
httpx[http2]>=0.27.0
numpy>=1.26.0
soundfile>=0.13.0
opuslib>=3.0.1
faster-whisper>=1.2.0
google-generativeai>=0.8.0
python-dotenv>=1.1.0
# TTS is HTTP-based, no local MeloTTS dependencies needed

# GPU-accelerated dependencies
# Install PyTorch with CUDA 12.1 support
--extra-index-url https://download.pytorch.org/whl/cu121
torch>=2.1.0
torchaudio>=2.1.0

# ONNX Runtime with CUDA support for Silero VAD
onnxruntime-gpu>=1.16.0

# CTranslate2 with CUDA support for Faster-Whisper
# Note: faster-whisper will automatically use GPU if available
